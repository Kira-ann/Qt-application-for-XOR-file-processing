# Описание тестовой программы
Программа модифицирует входные файлы бинарной операцией с заданным значением.
Порядок работы программы:

После запуска настраивается пользователем:
а) маска входных файлов, например .txt, testFile.bin
b) настройка необходимости удалять входные файлы или нет
c) путь для сохранения результирующих файлов
d) действия при повторении имени выходного файла: перезапись или модификация, например, счетчик к имени файла
e) работа по таймеру или разовый запуск
f) периодичность опроса наличия входного файла (таймер)
g) значение 8 байт для бинарной операции модификации файла
Функциональность: модифицирует входные файлы, операция XOR с 8-байтной переменной, введенной с формы (Пункт 1,g)

Обработка должна позволять обработать большого объёма файлы без "замирания" основной программы! 
Плюсом будет статус выполнения и прогресс выполнения обработки!

Среда разработки QT Creator, язык программирования С++, операционная система Windows, компилятор MinGW.
Результат работы должен содержать .pro или CMakeLists.txt файл обязательно.

# Пояснения реализации некоторых пунктов
a) В качестве маски использовались *.txt, testFile.bin. * - обозначение любого количества любых символов
с) По введеному пути сохраняется файл к базовому имени в конце добавляется xor (если входной - testFile.bin, то выходной - testFile_xor.bin)
d) В качестве модификации использовался счетчик(Если существует файл - testFile_xor.bin, следующим выходным файлом будет testFile_xor_1.bin)
f) Период задается в секундах
g) вводятся 16 hex-символов
Было использовано 2 потока, второстепенный поток реализовывал обработку файлов, через сигнал передавал текущее значение прогресса,
чтобы корректно его отобразить на шкале прогресса.

Статус обработки и прогресс были реализованы
Результат работы содержит CMakeLists.txt

## Окружение
Компилятор - MinGW 13.1.0
Make - 3.30.5
Qt - 6.11.0
